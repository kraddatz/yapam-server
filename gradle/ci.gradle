testSets {
    integrationTest
}

test {
    useJUnitPlatform {
    }
}

sonarqube {
    properties {
        property 'sonar.coverage.exclusions',
                "**/app/yapam/YapamApplication.java," +
                        "**/app/yapam/config/**," +
                        "**/app/yapam/common/error/**," +
                        "**/app/yapam/swagger/**," +
                        "**/app/yapam/**/model/**," +
                        "**/app/yapam/**/repository/*.java,"+
                        "**/app/yapam/common/service/storage_provider/**"
    }
}

configurations {
    cucumberRuntime {
        extendsFrom integrationTestCompile
    }
}

